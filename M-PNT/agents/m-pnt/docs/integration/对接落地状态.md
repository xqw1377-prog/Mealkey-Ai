# M-PNT 母体对接落地状态

> 更新: 2026-07-09  
> 策略: **方案 A** — 对外 7 步 Workflow 兼容母体；对内嵌三理论矩阵；终局映射 MKDecision

## 架构对齐

```text
母体 WorkflowEngine 7 步
  1 category_analysis      ≈ Situation 素材
  2 customer_portrait      ≈ 心智客户 / 场景
  3 price_positioning      ≈ 资源与价值匹配
  4 competitor_analysis    ≈ 空位与壁垒
  5 differentiation        ≈ Position + Ries/Trout/Ye 并行
  6 brand_tonality         ≈ Strategy 表达
  7 final_positioning      ≈ Cross-Fire → Synthesis → Quality
                              → fuse/map → MKDecision (+ M-Solution payload)
```

## 检查清单

| 项 | 状态 | 位置 |
|---|---|---|
| Manifest | ✅ | `packages/agents/src/m-pnt/manifest.ts` |
| Workflow 7 步 | ✅ | `packages/agents/src/m-pnt/workflow.ts` |
| 6 Capability | ✅ | `packages/agents/src/m-pnt/capabilities/*` |
| 系统提示词 | ✅ | `packages/agents/src/m-pnt/prompts/system.ts` |
| 报告模板 | ✅ | `packages/agents/src/m-pnt/reports/template.ts` |
| Agent 注册 | ✅ | `packages/agents/src/index.ts`（本仓 allAgents） |
| 意图检测 | ✅ 可合并 | `protocols/intent-detector.ts` → 合入 core |
| ProblemUnderstanding 映射 | ✅ 片段 | `mapPositioningProblem` |
| 领域知识种子 | ✅ | `knowledge/seeds.ts` |
| 测试 | ✅ | `packages/agents/tests/*` |
| 记忆读写闭环 | ⏳ 依赖母体 MemoryEngine | 接口已按规范预留 |
| AgentRuntime 真跑 | ⏳ 依赖母体 monorepo | 本仓提供 `runMPnt` 离线等价 |
| LLM hybrid 层 | ✅ | `m-pnt/llm/*`（heuristic / llm / hybrid） |
| Monorepo PR 清单 | ✅ | [MONOREPO-PR-清单.md](./MONOREPO-PR-清单.md) |
| 已拷入 Mealkey Agent | ✅ PR-1+部分 PR-2 | `Mealkey Agent/packages/agents/src/m-pnt` + intent/problem-understanding |
| **产品路径落地** | ✅ | `m-pnt.service` + agent 路由 + `/positioning` 页 + 决策/记忆/报告落库 |
| **定位结果卡 + profile 回写** | ✅ | `PositioningResultCard` + `project.profile.mPnt` + World 展示 + `latestPositioning` API |
| **会议挂载定位卡** | ✅ | Advisor 锚定区 + 一键「基于定位继续讨论」+ Chief 上下文注入 |
| **定位新旧 diff** | ✅ | `mPntPrevious`/`mPntHistory` + `PositioningDiffCard` + `positioningContext` API |
| **决策档案时间线 + 复审队列** | ✅ | `PositioningVersionTimeline` + `positioningReviewQueue` + Decisions 页徽章 |
| **复审一键开会 + Today 提醒** | ✅ | `buildReviewMeetingHref` + Advisor deep-link + Dashboard 定位复审卡 |
| **三理论各一 Agent 矩阵** | ✅ | `riesAgent`/`troutAgent`/`yeMaozhongAgent` 并行 → Cross-Fire → Synthesis |

## 合入 MealKey monorepo 步骤

1. 将 `packages/agents/src/m-pnt/**` 拷入 monorepo 同路径  
2. 删除本仓 `packages/agent-sdk` shim，改用真实 `@mealkey/agent-sdk`  
3. `index.ts` 中与 `LaunchAgent` 并列 `MPntAgent`  
4. `intent-detector.ts` 注册 `detectPositioningIntent`  
5. `problem-understanding` fallback 增加定位映射  
6. `runtime.registry.register(MPntAgent)`  
7. 用真实 LLM 替换 capability 内启发式（保留 payload 结构）
