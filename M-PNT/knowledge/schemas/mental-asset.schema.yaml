# MentalAsset — 心智语言、场景与联想资产
object_type: MentalAsset
fields:
  id: string
  asset_type: enum[
    mental_keywords,
    scene_triggers,
    first_association_terms,
    price_anchor_terms,
    emotional_value_terms
  ]
  term: string
  meaning: string
  target_customer_tags: list[string]
  scene_tags: list[string]
  category_tags: list[string]
  mental_effect: list[string]
  competitor_overlap_level: enum[low, medium, high]
  cultural_relevance: string
  usage_notes: string
