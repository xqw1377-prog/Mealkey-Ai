import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Apply · Mealkey Developers",
};

export default function ApplyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
