/**
 * useAgentChat — 已废弃
 *
 * @deprecated 此 hook 已被 advisor/page.tsx 中的内联流式逻辑替代。
 * advisor page 直接使用 fetch('/api/agent/stream') + ReadableStream reader，
 * 比此 hook 更完整地支持了资料上传、语音输入、工具调用等能力。
 *
 * 保留此文件仅用于引用，将在未来版本中移除。
 */
export {};
