{
  "name": "@mealkey/tool-agent-kit",
  "version": "0.1.0",
  "private": true,
  "description": "MealKey L3 Tool Agent Framework — Manifest / Ports / Registry / Compose（零业务依赖）",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "clean": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true })\""
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
