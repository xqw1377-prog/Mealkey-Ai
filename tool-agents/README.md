# tool-agents — L3 独立引擎目录

> 框架：`docs/MEALKEY_TOOL_AGENT_FRAMEWORK_V1.md` · `@mealkey/tool-agent-kit`

首个样板已落在 monorepo 包（非本目录子文件夹）：

| Agent ID | Package | 文档 |
|----------|---------|------|
| `m-ops-diag` | `@mealkey/m-ops-diag` | `docs/M_OPS_DIAG_AGENT_V1.md` |

后续 Agent 可二选一：

1. `packages/<name>`（与 m-ops-diag 同级，推荐）  
2. `tool-agents/<folder>/`（超多 Agent 时拆仓）

命名：产品族 `m-*` 或框架态 `l3.*`（均为 L3，不得升格顾问席）。
