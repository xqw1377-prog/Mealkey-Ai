{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm ci && node scripts/vercel-build-prep.mjs",
  "buildCommand": "npx turbo build --force",
  "outputDirectory": "apps/web/.next",
  "crons": [
    {
      "path": "/api/cron/reconcile-payments",
      "schedule": "0 2 * * *"
    }
  ]
}
